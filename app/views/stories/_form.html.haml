= simple_form_for(@story) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :description
    = f.input :status, :collection => Story.status_list
    = f.input :order_by
    = f.association :user

  .form-actions
    = f.button :submit
